/**
 * Rythme des entrées de navigation, partagé par le tiroir principal et celui
 * des réglages.
 *
 * Chez Twenty l'entrée de navigation est basse (28 px), en gris secondaire, et
 * ne s'allume qu'à l'état actif — un aplat gris, jamais une teinte. Les deux
 * tiroirs se relaient dans la même colonne : la constante vit ici pour qu'ils
 * ne puissent pas dériver l'un de l'autre.
 */
export const NAV_ITEM_CLASS = "h-7 gap-2 rounded-[4px] px-1.5 text-sm font-normal";
